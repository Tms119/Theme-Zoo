'use client';

export default function RevenueChart({ orders }) {
  return (
    <div style={{ height: 300, width: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--text-muted)' }}>
      Revenue Chart visualization is temporarily disabled until \`npm install\` is run.
    </div>
  );
}
